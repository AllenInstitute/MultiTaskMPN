# Repository instructions

- Save all test code under the top-level `test/` directory.
- Do not create test modules beside production code or at the repository root.
- Keep data-dependent manual checks in `test/` too, but avoid the `test_*.py`
  naming pattern when they should not run during automated test discovery.
